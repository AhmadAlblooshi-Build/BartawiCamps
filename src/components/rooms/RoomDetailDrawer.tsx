'use client'
// TODO: Full implementation in Section 9 (Session 5)
// Stub for forward reference from Section 8

interface Props {
  roomId: string
  onClose: () => void
}

export function RoomDetailDrawer({ roomId, onClose }: Props) {
  return null
}
